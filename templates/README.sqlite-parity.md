<!-- redline-testing template: sqlite parity README block -->
SQLite parity coverage and latency data are generated from raw JSONL evidence,
suite summaries, release manifests, and provenance hashes.
